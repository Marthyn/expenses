%li
  .project
    %span.color-dot{:style => "background-color:#{category.name.pastel_color};"}
    %span= category.name
    %span= "(#{category.monthly_budget})" if category.monthly_budget
    %span
      (#{link_to t("category.edit"), edit_category_path(category)})
